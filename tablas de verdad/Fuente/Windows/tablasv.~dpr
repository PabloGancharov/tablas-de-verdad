program tablasv;

uses
  Forms,
  tablas in 'tablas.pas' {Form1},
  analizador_de_tablas in 'analizador_de_tablas.PAS',
  analizador_sintactico in 'analizador_sintactico.pas',
  analizador_lexico in 'analizador_lexico.pas',
  Tipos in 'Tipos.pas';

{$R *.res}

begin
  Application.Initialize;
  Application.Title := 'Analizador de tablas de Verdad';
  Application.CreateForm(TForm1, Form1);
  Application.Run;
end.
